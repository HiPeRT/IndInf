#ifndef __CIRCULAR_ARRAY_H__
#define  __CIRCULAR_ARRAY_H__

/**
 * Push an element at the tail of the queue
 */
void q_push(int x);

/**
 * Pop an element from the head of the queue
 */
int q_pop();

#endif 