/*

int buff[N];
unsigned int size;

void q_push(int x)
{
    if(size > 0)
    {
        for(int i=size; i>0; i--)
            buff[i] = buff[i-1]
    }
    buff[0] = x;
    size++;
}


int pop_element()
{
    int ret = buff[size-1];
    size--;
    return ret;
}
*/